【基本形】状態を持つステート
状態があると何がいいの？
今までだと
1.レンダリング（HTMLを書く）時に値をぶっこむ
2．そうすると、HTMLにぶっこんだあたいが表示される
⇒でもそれまでです
3.じゃあ別の機会にコンポーネントを呼びだそうぜ！
あ、、、前回使った値を使いたいんだけど、、、
＊＊＊こういうときに役に立つのはstateである

＊＊＊＊＊コンポーネント内で変更した値をどこでも『取り出せる！』＊＊＊＊＊＊
＊＊＊＊＊Vueでいうemitは計算結果を親に渡していくイメージ。どこでも＜ではない＞『取り出せる！』＊＊＊＊＊＊
⇒reactだと、コールバックにてやり取りをする

あと、値を保持することもできる
前回入力
次回入力、、、、
1＋1＋1＋1＝4になる
class Timer extends React.Component {
  constructor(props) {
    super(props);
    this.state = { seconds: 0 };
  }

  tick() {
    this.setState(state => ({
      seconds: state.seconds + 1
    }));
  }

  componentDidMount() {
    this.interval = setInterval(() => this.tick(), 1000);
  }

  componentWillUnmount() {
    clearInterval(this.interval);
  }

  render() {
    return (
      <div>
        Seconds: {this.state.seconds}
      </div>
    );
  }
}

root.render(<Timer />);


https://ja.legacy.reactjs.org/