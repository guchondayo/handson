【React】elseが使えない時は&&もつかえまっせ(elseがないときって普通のJSでも&&で行けるっぽいね)
ファイルA
export default function MyButton(){
    return(<button>テスト</button>)
}
ファイルB
function Test(){
    let isOn = true
    return( {isOn && <MyButton />})
}

