【REACTで数値(文字列以外を渡す練習)】＊1.文字列以外を渡すときは{}だから気を付けてね
1.文字列以外を渡すときは{}だから気を付けてね

export default function Button(props){
    return(<button>{{ props.name }}</button>)
}
function Main (props){
    return(
            <div><p>{{ props.number}}</p>
                <Button name=props.name/>
            </div>
            )
}
<Main number={1000} />